Actúa como un experto en desarrollo Web y arquitecto de software. Vamos a crear una PWA 'Mobile First' para gestionar un parking de empresa de 8 plazas. Stack: React (Vite) + Tailwind CSS para frontend, Supabase para backend (Auth + Database). Reglas de negocio principales:

Hay 2 roles: Admin y User.

Hay exactamente 8 plazas fijas.

Los usuarios deben confirmar su reserva la noche anterior; si no, la pierden.

Los admins deben validar a los usuarios nuevos antes de que puedan reservar.

Los admins pueden bloquear plazas por mantenimiento.